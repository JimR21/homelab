services:
  traefik:
    image: traefik:v3.1
    container_name: traefik
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"  # Traefik dashboard
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /srv/dim-server/appdata/traefik:/etc/traefik
      - /srv/dim-server/appdata/traefik/certs:/etc/ssl/traefik
    command:
      # Enable Docker provider
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --providers.docker.network=traefik-private
      
      # Configure entrypoints
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      
      # Enable dashboard
      - --api.dashboard=true
      - --api.insecure=true
      
      # Configure certificates
      - --providers.file.directory=/etc/traefik
      - --providers.file.watch=true
      
      # No HTTPS redirect - using HTTP only for internal services
      
    labels:
      # Traefik dashboard
      - "traefik.enable=true"
      - "traefik.http.routers.traefik.rule=Host(`traefik.wouldyoukindly.enter`)"
      - "traefik.http.routers.traefik.entrypoints=web"
      - "traefik.http.services.traefik.loadbalancer.server.port=8080"
    networks:
      - traefik-private

networks:
  traefik-private:
    external: true